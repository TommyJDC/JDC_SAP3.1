[build]
      command = "npm run build"
      publish = "build/client"
      # functions = "netlify/functions" # Laissez l'adaptateur gérer cela

    [dev]
      command = "npm run dev"
      framework = "vite"

    [[redirects]]
      from = "/*"
      to = "/.netlify/functions/server"
      status = 200
