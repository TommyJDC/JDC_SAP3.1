[build]
  command = "npm run build" # Use the standard build command from package.json
  publish = "public"
  functions = "netlify/functions" # Default Netlify functions directory

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "no-cache"
